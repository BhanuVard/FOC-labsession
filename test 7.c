#include <stdio.h>

int main() {
    int n, sum;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    sum = n * n;

    printf("The sum of the odd number series is: %d", sum);

    return 0;
}
