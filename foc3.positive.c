#include <stdio.h>
int main() {
    int n;
   printf("Enter an integer: ");
    scanf("%d",&n);
    if(n>0)
    printf("positive:%d",n);
    else
  printf("negative:%d",n);

    return 0;
}

